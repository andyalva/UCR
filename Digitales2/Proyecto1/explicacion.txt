Bloque Opcional: Convierte secciones de 32 o 16 bits a 8 bits.

Entradas:
        Entradas de Control:
                 PCLK: Proviene del PLL. Su frecuencia depende de la tasa de transmisión y de la cantidad de bits de entada de la MAC.
                 Bit rate clk: Generado por el CLK para trasmitir y recivir.
        Entradas de Datos:
                 TxData [31:0], [15:0], [8:0]: Proviene de capa MAC. Vienen en formato paralelo.
Salidas:
        8 bits en paralelo que toma el encoder.

Bloque de ecoding:

Entradas:
        Entradas de Control:
                 TxDataK:
                 TxOnesZeros:
                 TxCompliance:
                 Bit rate clk:

        Salidas:
                10 bits de salida que sirven de entrada al convertidor de paralelo a serial.
